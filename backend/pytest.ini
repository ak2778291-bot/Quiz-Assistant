[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: requires a live Postgres with pgvector (set TEST_DATABASE_URL)
